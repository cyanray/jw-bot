#include <iostream>
#include <string>
#include "main.h"
#include <sstream>
#include <glog/logging.h>
using namespace std;
using namespace Cyan;

void CmdWeather(Message m)
{
	//string txt = m.MessageChain.GetPlainTextFirst();
	//if (txt != "查课表" && txt != "课表" && txt != "今天课表" && txt != "今日课表") return;

	//LOG(INFO) << "[" << m.Sender.ToInt64() << "] 使用 [...] 指令: " << txt;


}